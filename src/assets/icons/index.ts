/**
 * SVGs imported here become React components via the Metro transformer
 * configured in metro.config.js. They use `fill="currentColor"` so AppIcon's
 * `color` prop drives them from the theme rather than baking a colour in.
 */
export { default as HeartIcon } from './heart.svg';
export { default as StarIcon } from './star.svg';
